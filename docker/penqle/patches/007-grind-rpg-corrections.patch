diff --git a/ai/playerbot/TravelMgr.cpp b/ai/playerbot/TravelMgr.cpp
index 297c95a..f25c7f9 100644
--- a/ai/playerbot/TravelMgr.cpp
+++ b/ai/playerbot/TravelMgr.cpp
@@ -476,6 +476,12 @@ bool RpgTravelDestination::IsActive(Player* bot, const PlayerTravelInfo& info) c
     if (!IsPossible(info))
         return false;
 
+    // Taxi-cheat bots already use flight masters as route graph transitions.
+    // Sending them to one as an ambient roleplay destination adds a second,
+    // purposeless source of flight-master crowds and random taxi rides.
+    if (HasNpcFlag(UNIT_NPC_FLAG_FLIGHTMASTER) && bot->IsTaxiCheater())
+        return false;
+
     //Once the target rpged with it is added to the ignore list. We can now move on.
     std::set<ObjectGuid>& ignoreList = AI_VALUE(std::set<ObjectGuid>&,"ignore rpg target");
 
diff --git a/ai/playerbot/strategy/actions/ChooseRpgTargetAction.cpp b/ai/playerbot/strategy/actions/ChooseRpgTargetAction.cpp
index 2409cc2..9322663 100644
--- a/ai/playerbot/strategy/actions/ChooseRpgTargetAction.cpp
+++ b/ai/playerbot/strategy/actions/ChooseRpgTargetAction.cpp
@@ -15,12 +15,12 @@
 
 using namespace ai;
 
-bool ChooseRpgTargetAction::HasSameTarget(ObjectGuid guid, uint32 max, std::list<ObjectGuid>& nearGuids)
+std::unordered_map<ObjectGuid, uint32> ChooseRpgTargetAction::GetTargetCounts(std::list<ObjectGuid> const& nearGuids)
 {
-    if (ai->HasRealPlayerMaster())
-        return false;
+    std::unordered_map<ObjectGuid, uint32> counts;
 
-    uint32 num = 0;
+    if (ai->HasRealPlayerMaster())
+        return counts;
 
     for (auto& i : nearGuids)
     {
@@ -32,24 +32,20 @@ bool ChooseRpgTargetAction::HasSameTarget(ObjectGuid guid, uint32 max, std::list
         if (!ai->IsSafe(player))
             continue;
 
-        PlayerbotAI* ai = PlayerbotAIStorage::Instance().GetAI(player);
+        PlayerbotAI* otherAI = PlayerbotAIStorage::Instance().GetAI(player);
 
-        if (!ai)
+        if (!otherAI)
             continue;
 
-        if (!ai->AllowActivity(GRIND_ACTIVITY))
+        if (!otherAI->AllowActivity(GRIND_ACTIVITY))
             continue;
 
-        if (PAI_VALUE(GuidPosition,"rpg target") != guid)
-            continue;
-
-        num++;
-
-        if (num >= max)
-            return true;
+        GuidPosition target = PAI_VALUE(GuidPosition, "rpg target");
+        if (target)
+            ++counts[target];
     }
 
-    return false;
+    return counts;
 }
 
 #define SkipRpgTarget(reason){ if(debug) debugTargets[guidP] = reason; continue;}
@@ -74,6 +70,12 @@ std::unordered_map<ObjectGuid, float> ChooseRpgTargetAction::GetTargets(Player*
     std::list<ObjectGuid> possibleObjects = bot->GetMap()->IsDungeon() ? AI_VALUE(std::list<ObjectGuid>, "nearest game objects") : AI_VALUE(std::list<ObjectGuid>, "nearest game objects no los");
     std::list<ObjectGuid> possiblePlayers = AI_VALUE(std::list<ObjectGuid>, "nearest friendly players");
 
+    // Count the bots per rpg target once before sampling players. The old
+    // per-candidate rescan gave up entirely at 200 neighbors, which is exactly
+    // where crowd avoidance is needed most; the tally keeps the crowd limit
+    // active at every population and costs one pass instead of one per target.
+    auto targetCounts = GetTargetCounts(possiblePlayers);
+
     //List of targets that we rpg'ed with before and should be ignored.
     std::set<ObjectGuid>& ignoreList = AI_VALUE(std::set<ObjectGuid>&, "ignore rpg target");
 
@@ -228,8 +230,9 @@ std::unordered_map<ObjectGuid, float> ChooseRpgTargetAction::GetTargets(Player*
             }
         }
 
-        //Limit the amount of bots that can rpg with 1 target. Only if the calculation doesn't involve checking 200+ players.
-        if (possiblePlayers.size() < 200 && HasSameTarget(guidP, urand(5, 15), possiblePlayers))
+        //Keep the native crowd limit without rescanning neighbors per candidate.
+        auto occupancy = targetCounts.find(guidP);
+        if (occupancy != targetCounts.end() && occupancy->second >= urand(5, 15))
         {
             sametarget++;
             SkipRpgTarget("Too many bots are rpging with this npc.");
@@ -512,8 +515,10 @@ bool ChooseRpgTargetAction::Execute(Event& event)
     }
 
     //We pick a random target from the list with targets having a higher relevance of being picked.
-    std::mt19937 gen(time(0));
-    WeightedShuffle(guidps.begin(), guidps.end(), relevances.begin(), relevances.end(), gen);
+    //Use the shared thread-local generator instead of a per-call time(0)-seeded
+    //engine: re-seeding every call made consecutive choices correlate and the
+    //pick hard to reproduce in tests.
+    WeightedShuffle(guidps.begin(), guidps.end(), relevances.begin(), relevances.end(), *GetRandomGenerator());
     GuidPosition guidP(guidps.front(),bot->GetMapId(), bot->GetInstanceId());
 
     //If we can't find a target clear ignore list and try again later.
diff --git a/ai/playerbot/strategy/actions/ChooseRpgTargetAction.h b/ai/playerbot/strategy/actions/ChooseRpgTargetAction.h
index e67f3de..4ee073c 100644
--- a/ai/playerbot/strategy/actions/ChooseRpgTargetAction.h
+++ b/ai/playerbot/strategy/actions/ChooseRpgTargetAction.h
@@ -19,7 +19,7 @@ namespace ai
         std::string GetRpgActionReason(ObjectGuid target) { auto reason = rgpActionReason.find(target); if (reason != rgpActionReason.end())  return reason->second; return ""; }
     private:
         float getMaxRelevance(GuidPosition guidP);
-        bool HasSameTarget(ObjectGuid guid, uint32 max, std::list<ObjectGuid>& nearGuids);
+        std::unordered_map<ObjectGuid, uint32> GetTargetCounts(std::list<ObjectGuid> const& nearGuids);
 
         std::unordered_map <ObjectGuid, std::string> rgpActionReason;
     };
diff --git a/ai/playerbot/strategy/actions/MoveToRpgTargetAction.cpp b/ai/playerbot/strategy/actions/MoveToRpgTargetAction.cpp
index a0efbb1..cb816e8 100644
--- a/ai/playerbot/strategy/actions/MoveToRpgTargetAction.cpp
+++ b/ai/playerbot/strategy/actions/MoveToRpgTargetAction.cpp
@@ -26,7 +26,12 @@ bool MoveToRpgTargetAction::Execute(Event& event)
     else if(go)
         wo = go;
     else
+    {
+        // Drop the vanished target so the next choice can make progress
+        // instead of re-issuing a move leg against nothing.
+        RESET_AI_VALUE(GuidPosition, "rpg target");
         return false;
+    }
 
     if (guidP.IsPlayer())
     {
@@ -165,6 +170,12 @@ bool MoveToRpgTargetAction::Execute(Event& event)
         {
             ai->TellDebug(GetMaster(), "Can not path to desired location around " + chat->formatWorldobject(guidP.GetWorldObject(bot->GetInstanceId())) + " trying again later.", "debug move");
 
+            // Reuse the native target rejection so the next choice can make
+            // progress instead of re-rolling the same unreachable approach
+            // point every tick.
+            AI_VALUE(std::set<ObjectGuid>&, "ignore rpg target").insert(guidP);
+            RESET_AI_VALUE(GuidPosition, "rpg target");
+
             return false;
         }
     }
@@ -232,6 +243,10 @@ bool MoveToRpgTargetAction::isUseful()
         {
             ai->TellPlayerNoFacing(GetMaster(), "Target could not be found. Drop rpg target");
         }
+
+        // Stop the move leg here. Falling through made the action still count
+        // as useful and chain into the next patrol leg with a stale target.
+        return false;
     }
 
     if(MEM_AI_VALUE(WorldPosition, "current position")->LastChangeDelay() < 60)
diff --git a/ai/playerbot/strategy/actions/MovementActions.h b/ai/playerbot/strategy/actions/MovementActions.h
index 11a4fbd..d475e94 100644
--- a/ai/playerbot/strategy/actions/MovementActions.h
+++ b/ai/playerbot/strategy/actions/MovementActions.h
@@ -9,6 +9,19 @@
 #include "playerbot/strategy/values/HazardsValue.h"
 #include "playerbot/strategy/values/LastMovementValue.h"
 
+// Move priorities, for code ported from mod-playerbots, which arbitrates
+// moves by them. Nothing in this tree arbitrates: MoveTo takes the move and
+// issues it. The type exists so ported call sites keep their shape, and every
+// place that drops the value says so (same shape the donor host gave its own
+// MovementAction).
+enum class MovementPriority : uint8
+{
+    MOVEMENT_IDLE   = 0,
+    MOVEMENT_NORMAL = 1,
+    MOVEMENT_COMBAT = 2,
+    MOVEMENT_FORCED = 3
+};
+
 namespace ai
 {
     class MovementAction : public Action
@@ -31,7 +44,12 @@ namespace ai
            return MoveTo(location.mapId, location.x, location.y, location.z, idle, react, noPath, ignoreEnemyTargets);
         }
 
-        static bool UseTaxi(PlayerbotAI* ai, uint32 entry = 0, bool needNpc = true);
+    public:
+        // Static taxi adapter shared with the RPG actions (which are not
+        // MovementAction subclasses).
+        static bool UseTaxi(PlayerbotAI* ai, uint32 entry = 0, bool needNpc = true, Creature* sourceNpc = nullptr);
+
+    protected:
 
 
         bool WaitForTransport();
@@ -85,10 +103,51 @@ namespace ai
         bool ShouldReactionInterruptCast() const override { return true; }
         bool ShouldReactionInterruptMovement() const override { return true; }
 
-    private:
+    protected: // the dungeon-clear port's dropping overloads are used by subclasses
         bool IsValidPosition(const WorldPosition& position, const WorldPosition& visibleFromPosition);
         bool IsHazardNearPosition(const WorldPosition& position, HazardPosition* outHazard = nullptr);
         bool GeneratePathAvoidingHazards(std::vector<WorldPosition>& movePath);
+
+        // --- dungeon-clear port: mod-playerbots movement surface ------------
+        // MoveTo overload dropping the mod-playerbots movement arguments
+        // (see MovementPriority above and the donor host's MovementAction).
+
+        // mod-playerbots carries four more arguments on this call. Three are
+        // accepted and dropped - exact_waypoint, lessDelay and backwards have
+        // no counterpart in this generator - and the priority goes the way
+        // every other priority here goes. normal_only maps onto noPath, which
+        // is the same request: do not route, go straight.
+        bool MoveTo(uint32 mapId, float x, float y, float z, bool idle, bool react,
+                    bool normal_only, bool /*exact_waypoint*/,
+                    MovementPriority /*priority*/, bool /*lessDelay*/ = false,
+                    bool /*backwards*/ = false)
+        { return MoveTo(mapId, x, y, z, idle, react, normal_only); }
+
+        // mod-playerbots refuses a move to a spot the bot was just sent to.
+        // Nothing here records that, so nothing is a duplicate. False keeps
+        // the move flowing, which is the safe direction - see
+        // IsWaitingForLastMove.
+        bool IsDuplicateMove(float /*x*/, float /*y*/, float /*z*/) const { return false; }
+
+        // mod-playerbots spelling: jump the bot to a point. Sits on
+        // MotionMaster::MoveJump. The mapId is accepted and ignored - a jump
+        // never leaves the map, and ported callers pass it because their
+        // MoveTo takes one. The priority is dropped.
+        bool JumpTo(uint32 /*mapId*/, float x, float y, float z,
+                    MovementPriority /*priority*/ = MovementPriority::MOVEMENT_NORMAL);
+
+        // mod-playerbots records a wait sized to the last leg and refuses a
+        // later move whose priority is not strictly greater. This tree
+        // records no such wait - LastMovement carries no timestamp and no
+        // priority - so there is nothing to answer from and this says "not
+        // waiting". False is the safe answer, not merely the convenient one:
+        // the failure it avoids is a leftover wait silently refusing the
+        // NEXT leg, leaving the tank standing in the pack it just pulled.
+        bool IsWaitingForLastMove(MovementPriority /*priority*/ = MovementPriority::MOVEMENT_NORMAL) const { return false; }
+
+        // No-argument form: may the bot start moving from where it stands.
+        bool IsMovingAllowed();
+        // --------------------------------------------------------------------
     };
 
     class FleeAction : public MovementAction
diff --git a/ai/playerbot/strategy/actions/RpgAction.cpp b/ai/playerbot/strategy/actions/RpgAction.cpp
index fac0da4..091f0d7 100644
--- a/ai/playerbot/strategy/actions/RpgAction.cpp
+++ b/ai/playerbot/strategy/actions/RpgAction.cpp
@@ -127,9 +127,10 @@ bool RpgAction::SetNextRpgAction()
         }
     }
 
-    std::mt19937 gen(time(0));
-
-    WeightedShuffle(actions.begin(), actions.end(), relevances.begin(), relevances.end(), gen);
+    //Use the shared thread-local generator instead of a per-call time(0)-seeded
+    //engine: re-seeding every call made consecutive choices correlate and the
+    //pick hard to reproduce in tests.
+    WeightedShuffle(actions.begin(), actions.end(), relevances.begin(), relevances.end(), *GetRandomGenerator());
 
     Action* action = actions.front();
 
diff --git a/ai/playerbot/strategy/actions/RpgSubActions.cpp b/ai/playerbot/strategy/actions/RpgSubActions.cpp
index 45ef8f0..7cd70c6 100644
--- a/ai/playerbot/strategy/actions/RpgSubActions.cpp
+++ b/ai/playerbot/strategy/actions/RpgSubActions.cpp
@@ -11,6 +11,7 @@
 #include "GuildCreateActions.h"
 #include "SocialMgr.h"
 #include "playerbot/TravelMgr.h"
+#include "MovementActions.h"
 #include "SayAction.h"
 #include "playerbot/PlayerbotLLMInterface.h"
 
@@ -195,7 +196,6 @@ bool RpgTaxiAction::Execute(Event& event)
 
     uint32 path = nodes[urand(0, nodes.size() - 1)];
     uint32 money = bot->GetMoney();
-    bot->SetMoney(money + 100000);
 
     TaxiPathEntry const* entry = sTaxiPathStore.LookupEntry(path);
     if (!entry)
@@ -219,16 +219,22 @@ bool RpgTaxiAction::Execute(Event& event)
         bot->SetMoney(money);
         return false;
     }
-    if (!bot->ActivateTaxiPathTo({ entry->from, entry->to }, flightMaster, 0))
+
+    // Keep the existing ambient free-flight policy, but use the SAME native
+    // eligibility/discovery adapter as purposeful travel. Fund only the exact
+    // fare and restore funds on rejection too; a temporary credit larger than
+    // the fare both overdraws the bot ledger and, on the old early-return
+    // paths, used to leak.
+    bot->SetMoney(uint32(std::min<uint64>(uint64(money) + entry->price, uint64(uint32(-1)))));
+    bool const activated = MovementAction::UseTaxi(ai, path, true, flightMaster);
+    bot->SetMoney(money);
+    if (!activated)
     {
         sLog.outError("Bot %s cannot fly %u (%zu location available)", bot->GetName(), path, nodes.size());
-        bot->SetMoney(money);
         return false;
     }
 
-
     sLog.outDetail("Bot #%d <%s> is flying from %s to %s (%zu location available)", bot->GetGUIDLow(), bot->GetName(), nodeFrom->name[LOCALE_enUS], nodeTo->name[LOCALE_enUS], nodes.size());
-    bot->SetMoney(money);
 
     rpg->AfterExecute();
 
diff --git a/ai/playerbot/strategy/values/PossibleRpgTargetsValue.cpp b/ai/playerbot/strategy/values/PossibleRpgTargetsValue.cpp
index cf82a5a..3af0d78 100644
--- a/ai/playerbot/strategy/values/PossibleRpgTargetsValue.cpp
+++ b/ai/playerbot/strategy/values/PossibleRpgTargetsValue.cpp
@@ -47,6 +47,11 @@ bool PossibleRpgTargetsValue::AcceptUnit(Unit* unit)
 {
     TravelTarget* travelTarget = context->GetValue<TravelTarget*>("travel target")->Get();
 
+    // Flight masters remain available to purposeful travel/discovery, but a
+    // taxi-cheat bot must not pick one as generic nearby roleplay work.
+    if (unit->HasFlag(UNIT_NPC_FLAGS, UNIT_NPC_FLAG_FLIGHTMASTER) && bot->IsTaxiCheater())
+        return false;
+
     if (travelTarget->GetDestination() && travelTarget->GetDestination()->GetEntry() == unit->GetEntry())
         return true;
 
